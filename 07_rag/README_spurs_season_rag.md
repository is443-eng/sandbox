# Spurs season RAG (canonical location moved)

All Spurs season RAG **code and documentation** for this lab live under **`08_function_calling/`**:

- **[`../08_function_calling/README_spurs_season_rag.md`](../08_function_calling/README_spurs_season_rag.md)** — full README
- **`../08_function_calling/spurs_season_rag.py`** — CLI (this folder’s `spurs_season_rag.py` is a shim that forwards to that file)

From here you can still run:

```bash
pip install -r requirements_spurs_rag.txt
python spurs_season_rag.py --refresh
```

…which delegates to `08_function_calling/spurs_season_rag.py`.

---

← [LAB: Create Your Own RAG AI Query](LAB_custom_rag_query.md)
